<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="view.css" />
    <title>Login</title>
  </head>
  <body>
    <form action="/users/login" method="POST">
      <h1>Member Login</h1>
      <input
        type="email"
        id="email"
        name="email"
        placeholder="Enter Email"
        required
      />

      <input
        type="password"
        id="password"
        name="password"
        placeholder="Password"
        required
      />

      <input type="submit" class="button" value="Login" />

      <a href="/users/register">Not yet member? Signup now</a>
    </form>

    <style>
      * {
        padding: 0;
        margin: 0;
        box-sizing: border-box;
      }
      body {
        max-width: 500px;
        margin: auto;
        background: #f5f5f5;
        padding: 10px;
      }
      h1 {
        margin: 50px;
        font-family: sans-serif;
        color: #1d6868;
        font-size: 36px;
      }

      form {
        display: flex;
        flex-direction: column;
        align-items: center;
        max-width: 481px;
        height: 500px;
        background-color: #92c6c6;
        border-radius: 30px;
        text-align: center;
        margin-top: 50px;
      }

      form input {
        width: 303px;
        height: 50px;
        border-radius: 20px;
        margin: 10px;
        background-color: #d9d9d9;
        color: #6d5858;
        text-align: left;
        padding: 5px;
        border: none;
        font-size: 15px;
      }

      .button {
        width: 151px;
        height: 37px;
        border-radius: 20px;
        background-color: #63a4a4;
        cursor: pointer;
        text-align: center;
        color: black;
        font-size: 15px;
        font-weight: bold;
      }
    </style>
  </body>
</html>
